import 'package:flutter/material.dart';

class ContinuewithPhoneNumber extends StatelessWidget {
  const ContinuewithPhoneNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}